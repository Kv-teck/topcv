export interface Language {
    id: number;                      // Unique identifier for the language
    name: string;                    // Name of the language
    proficiency: string;             // Proficiency level (e.g., beginner, intermediate, fluent)
}